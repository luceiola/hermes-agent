model:
  provider: newapi
  default: ${MODEL}

providers:
  newapi:
    base_url: ${BASE_URL}
    wire_api: responses
    key_env: API_KEY

agent:
  max_iterations: 24

display:
  personality: neutral

skills:
  disabled: []
  platform_disabled: {}

telegram:
  channel_prompts: {}

slack:
  channel_prompts: {}

discord:
  channel_prompts: {}
  require_mention: true
